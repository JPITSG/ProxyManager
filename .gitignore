# Built package — rebuild from source (see README.md)
proxy-manager.xpi

# AMO listing assets (generated from icons/icon.svg)
assets/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*~

# Tooling / dependency directories (if introduced later)
node_modules/

# Signing workflow — credentials, signed artifacts, local runbook
.amo-credentials
.amo-upload-uuid
web-ext-artifacts/
AGENTS.md
